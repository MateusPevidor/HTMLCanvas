declare class Utils {
    static createMatrix(rows: number, columns: number): number[][];
    static multiplyMatrices(m: number[][], n: number[][]): {};
    static sumMatrices(m: number[][], n: number[][]): {};
    static Sigmoid(n: number): number;
}
export default Utils;
